{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/server.py"
        }
    ],
    "builds": [
        {
            "src": "api/server.py",
            "use": "@vercel/python"
        }
    ]
}